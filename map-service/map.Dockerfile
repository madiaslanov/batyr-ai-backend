# Используем официальный образ Python
FROM python:3.9-slim

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем файл с зависимостями
# ✅ ИСПРАВЛЕНИЕ 1: Используем правильное имя с нижним подчеркиванием
COPY map.requirements.txt .

# Устанавливаем зависимости
# ✅ ИСПРАВЛЕНИЕ 1 (продолжение): Используем правильное имя
RUN pip install --no-cache-dir -r map.requirements.txt

# Копируем код приложения и файл с данными в контейнер
# ✅ ИСПРАВЛЕНИЕ 2: Используем правильное имя с маленькой 'b'
COPY mapBatyr.py .
COPY batyrs_data.json .

# Указываем команду для запуска приложения
# ✅ ИСПРАВЛЕНИЕ 2 (продолжение): Имя здесь уже было правильным, но теперь оно совпадает с COPY
CMD ["python", "-u", "mapBatyr.py"]